import { Inter, Rubik } from 'next/font/google';

export const inter = Inter({
    subsets: ['latin'],
    display: 'swap'
});
export const rubik = Rubik({
    subsets: ['latin'],
    display: 'swap'
});